# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32

# Base de datos
SQLAlchemy[asyncio]==2.0.54
asyncpg==0.31.0
alembic==1.20.0
greenlet==3.5.6

# Colas
taskiq==0.12.6
taskiq-redis==1.2.3
redis==8.1.0

# XML y firma
#
# xmlsec y lxml CONGELADOS Y COMPILADOS DESDE FUENTE (ver Dockerfile:
# `pip install --no-binary lxml,xmlsec`). Los wheels precompilados de ambos
# traen su propia copia estática de libxml2; cargar los dos en el mismo proceso
# deja dos libxml2 distintas en memoria y el primer `xmlsec.sign()` sobre un
# árbol creado por lxml termina en segfault, no en excepción. Compilados contra
# la libxml2/libxmlsec1 del sistema (las que instala el Dockerfile), comparten
# una sola. Este par de versiones es el verificado contra esa imagen base:
# subir cualquiera de las dos exige volver a correr tests/test_signer.py dentro
# del contenedor, no en el host.
lxml==6.1.3
xmlsec==1.3.17
cryptography==50.0.1

# Cliente SII
httpx[http2]==0.28.1
tenacity==9.1.4

# Almacenamiento de objetos
aioboto3==15.5.0

# Observabilidad
prometheus-client==0.26.0
sentry-sdk[fastapi]==2.70.0
